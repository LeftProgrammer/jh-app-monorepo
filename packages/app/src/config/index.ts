/**
 * 配置模块统一导出
 * 
 * 使用方式：
 * import { manifestConfig, pagesConfig, unoConfig } from '@jh-app/app/config'
 */

// 直接导出配置
export { manifestConfig } from './manifest'
export { pagesConfig } from './pages'
export { unoConfig } from './uno'
